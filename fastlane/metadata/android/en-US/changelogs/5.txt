Prepping the FOSS build for F-Droid.

Wakes via UnifiedPush (no Firebase or Play Services), reports crashes via ACRA,
and keeps an on-device audit log of every tool call. On-demand MCP server with
per-integration opt-in.

Also: connection timeout settings, and a fix for the background-delivery
notification header.
